#include "../../imports/stdlib.fc";

() test(slice in_msg) impure method_id (1) {
    int flags = 0;
    int n = 3;
    int count = 0;
    while (count < n) {
        if (in_msg~load_uint(1)) {
            flags += 1;
        }
        count = count + 1;
    }
    throw_if(400, flags == n);
}

() main(slice in_msg) {
    test(in_msg);
    return ();
}
