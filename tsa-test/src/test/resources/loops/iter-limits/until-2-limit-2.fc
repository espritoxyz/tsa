#include "../../imports/stdlib.fc";

() test(slice in_msg) impure method_id (1) {
    int flags = 0;
    int n = 2;
    int count = 0;
    do {
        if (in_msg~load_uint(1)) {
            flags += 1;
        }
        count += 1;
    } until (count >= n);
    throw_if(300, flags == n);
}

() main(slice in_msg) {
    test(in_msg);
    return ();
}
