#include "../imports/stdlib.fc";

(int) is_prefix_rev(slice, slice) impure method_id (100) asm "SDPFXREV";

(int) is_prefix(slice a, slice b) impure method_id (101) {
    return is_prefix_rev(b, a);
}

(int) is_pfx_zeroes() impure method_id (0) {
    slice a = begin_cell().store_uint(0, 4).end_cell().begin_parse();
    slice b = begin_cell().store_uint(0, 8).end_cell().begin_parse();
    return is_prefix(a, b);
}


(int) is_pfx_zeroestwo() impure method_id (1) {
    slice a = begin_cell().store_uint(0, 4).end_cell().begin_parse();
    slice b = begin_cell().store_uint(0, 8).end_cell().begin_parse();
    return is_prefix(b, a);
}

(int) is_pfx_one() impure method_id (2) {
    slice a = begin_cell().store_uint(1, 4).end_cell().begin_parse();
    slice b = begin_cell().store_uint(1 + 32, 8).end_cell().begin_parse();
    return is_prefix(a, b);
}


(int) is_pfx_one_not_prefix() impure method_id (3) {
    slice a = begin_cell().store_uint(1, 4).end_cell().begin_parse();
    slice b = begin_cell().store_uint(0, 8).end_cell().begin_parse();
    return is_prefix(a, b);
}




