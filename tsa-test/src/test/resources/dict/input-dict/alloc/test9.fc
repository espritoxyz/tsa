#include "../../../imports/stdlib.fc";

slice wrap(int arg) {
    return begin_cell().store_uint(arg, 64).end_cell().begin_parse();
}
() main(slice in_msg) {
    var dict = new_dict();
    dict~udict_set(4, 0, wrap(3));
    int found = dict~udict_delete?(4, 16);
    if (found) {
        throw(101);
    } else {
        throw(102);
    }
    return ();
}
