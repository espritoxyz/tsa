#include "../../../imports/stdlib.fc";

() test(slice in_msg) impure method_id (1) {

    cell init_dict = in_msg~load_dict();
    (slice value, int found) = init_dict.udict_get?(64, 37);
    throw_unless(300, found); ;; assert exists
    throw_unless(301, value.preload_int(64) == 17);

    (cell delete_dict, slice second_value, int found1) = udict_delete_get?(init_dict, 64, 37);
    throw_unless(400, found1);
    throw_unless(401, second_value.preload_int(64) == 17);

    (_, int found2) = delete_dict.udict_get?(64, 37);
    throw_unless(402, ~ found2);
    throw(500); ;; successfull execution
}

() main(slice in_msg) {
    test(in_msg);
    return ();
}
