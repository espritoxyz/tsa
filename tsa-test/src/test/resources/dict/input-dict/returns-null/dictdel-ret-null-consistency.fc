#include "../../../imports/stdlib.fc";

() test(slice in_msg) impure method_id (1) {
    cell init_dict = in_msg~load_dict();
    throw_unless(300, ~ null?(init_dict));
    (cell del_dict, int deleted) = udict_delete?(init_dict, 64, 37);
    throw_unless(301, deleted);
    throw_unless(302, null?(del_dict));
    (_, _, int found) = init_dict.udict_get_prev?(64, 37);
    throw_unless(400, ~ found);
    (_, _, int found2) = init_dict.udict_get_next?(64, 37);
    throw_unless(401, ~ found2);
    throw(500);
}

() main(slice in_msg) {
    test(in_msg);
    return ();
}
