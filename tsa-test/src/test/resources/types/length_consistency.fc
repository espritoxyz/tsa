#include "../imports/stdlib.fc";

() recv_internal(int msg_value, cell in_msg_cell, slice in_msg) impure {
    int msg_length = in_msg.slice_bits();
    int coins = in_msg~load_coins();
    coins~impure_touch();
    throw_if(1000, msg_length <= 2); ;; should be unreachable
    throw(1001);
}
