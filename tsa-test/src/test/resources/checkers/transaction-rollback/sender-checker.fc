#include "../../imports/stdlib.fc";
#include "../../imports/tsa_functions.fc";


() main() impure {
    var data1 = tsa_get_c4(1).begin_parse();
    var int1 = data1~load_uint(1);
    throw_if(257, (int1 != 0));
    tsa_send_internal_message(1, 0);
    var data2 = tsa_get_c4(1).begin_parse();
    var int2 = data2~load_uint(1);
    throw_if(258, (int2 == 0));
}
