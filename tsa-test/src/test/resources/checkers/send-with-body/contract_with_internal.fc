#include "../../imports/stdlib.fc";

() recv_internal(slice msg_body) impure {
    slice ds = get_data().begin_parse();
    int old_x = ds~load_uint(32);
    int new_x = msg_body~load_uint(32);

    throw_unless(1000, old_x == new_x);

    set_data(begin_cell().store_uint(new_x, 32).end_cell());
}
