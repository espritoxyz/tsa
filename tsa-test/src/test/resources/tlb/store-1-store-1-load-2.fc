#include "../imports/stdlib.fc";


() main() {
    var stored = begin_cell().store_uint(1, 1).store_uint(1, 1).end_cell();
    (_, var load) = stored.begin_parse().load_uint(2);
    throw_unless(400, load == 3);
    throw(500);
}
